interface:
  display_name: "OKF Route Pack Initialiser"
  short_description: "Bootstrap OKF route-pack infrastructure."
  default_prompt: "Use $okf-wiki-initialiser to plan or perform OKF route-pack bootstrap for this repository; do not write tracked files, backfill, or rebuild generated readers until active-mode implementation/tracked-doc authority exists."

policy:
  allow_implicit_invocation: true
