interface:
  display_name: "OKF Router"
  short_description: "Select OKF route evidence for repo work."
  default_prompt: "Use $okf-router to choose the relevant OKF route card as routing evidence before broad source inspection; follow separate requirements, planning, approval, and implementation gates."

policy:
  allow_implicit_invocation: true
